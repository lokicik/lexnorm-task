[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ttsg-pipeline"
version = "0.1.0"
description = "Deterministic OCR and consolidation pipeline for TTSG filings."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["ttsg*"]
